{{! <div>
    <form action="/breed-search-results" method="GET">
        <input type="text" name="breed" placeholder="Search for a breed">
        <button>Search for a Breed</button>
    </form>
</div> }}

<div>

  <select onchange="location = this.value;">
    {{#each allBreeds}}
      <option value="/breed/{{this._id}}">
        {{this.name}}</option>
    {{/each}}
  </select>
</div>