<header>
  <div>
    <a href="/">Dogs 4 Life</a>
  </div>
  <nav>
    <a href="/">Home</a>
    <a href="/breeds">Find a breed</a>
     <a href="/trending">Trending</a>
    {{#if currentUser}}
      <a href="/favorites">My Favorite Breeds</a>
    {{/if}}
  </nav>
  <nav>
    {{#if currentUser}}
      <a href="/profile">{{currentUser.username}}</a>
      <a href="/logout">Logout</a>
    {{/if}}

    {{#unless currentUser}}
      <a href="/login">Login</a>
      <a href="/signup">Signup</a>
    {{/unless}}
  </nav>
</header>
<div class="secondary-background-div">
  <div class="secondary-box">
    <div>
      <h2>Find a Breed</h2>
    </div>
    <br>
    <div>
      <select onchange="location = this.value;">
        {{#each allBreeds}}
          <option value="/breed/{{this._id}}">
            {{this.name}}
          </option>
        {{/each}}
      </select>
    </div>
  </div>
</div>